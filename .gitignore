# Redundant packaged duplicate of FanSafe_PWA/ — regenerated ad hoc, not a
# build artifact of any tooling in this repo, and would otherwise duplicate
# the whole app's bytes in every commit that touches it. See README.md.
FanSafe_PWA.zip

# OS/editor noise
.DS_Store
Thumbs.db
*.swp

# Playwright CLI scratch output (console logs, ad hoc screenshots/snapshots)
# from manual browser smoke-testing sessions — not build artifacts. Curated
# evidence lives in docs/release-evidence/, which IS tracked.
.playwright-cli/
